require 'spec_helper'
require 'heros'

RSpec.describe Weapon, "#init" do
  it "should equal 2 + 2" do
    expect(2).to eq(2)
  end
end

RSpec.describe Hero, "#init" do
  it "should equal 2 + 2" do
    expect(2).to eq(2)
  end
end
